import type { Role } from "../types";

/** Colonnes de la matrice rôles × modules (les comptes, eux, viennent de l'API). */
export const ADMIN_ROLES: Role[] = [
  "admin",
  "dg",
  "dir_commercial",
  "dir_operations",
  "presale",
  "dir_financier",
  "commercial",
];
